version: '3.7'

services:
    clematis_auth_api:
        image: clematis.auth.api:latest
        env_file:
          - clematis_auth_api.env
        volumes:
          - /home/clematis/logs:/var/log/clematis
        ports:
          - "8083:8083"
        restart: unless-stopped

